{
  "name": "etherking",
  "version": "2.0.0",
  "private": true,
  "description": "A secure, account-based multi-model chat application.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/captcha.js && node --check lib/security.js && node --check lib/sse.js && node --check lib/ndjson.js && node --check lib/providers.js && node --check lib/quota.js && node --check lib/storage.js && node --check public/assets/auth.js && node --check public/assets/app-v2.js",
    "test": "node --test --test-isolation=none"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}

