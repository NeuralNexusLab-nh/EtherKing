# Security policy

Do not report vulnerabilities in public issues. Contact the repository owner privately with reproduction steps, affected endpoints, and the expected impact.

Production operators should use HTTPS, managed PostgreSQL backups, encrypted environment secrets, provider-side spending limits, and centralized monitoring that excludes message bodies and credentials.

