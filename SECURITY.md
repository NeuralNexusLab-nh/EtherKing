# Security policy

Do not report vulnerabilities in public issues. Contact the repository owner privately with reproduction steps, affected endpoints, and the expected impact.

Production operators should use HTTPS, protected environment secrets, provider-side spending limits, persistent filesystem backups, restrictive filesystem permissions, and centralized monitoring that excludes message bodies and credentials. The Base64 storage format is obfuscation, not encryption.

